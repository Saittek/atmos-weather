# Production / iOS build: public URL of your hosted Atmos auth API (no trailing slash)
# Example: https://atmos-api.yourdomain.com
VITE_API_BASE=

# Optional JWT secret for the Node server (production)
# JWT_SECRET=change-me-to-a-long-random-string
# PORT=8787
